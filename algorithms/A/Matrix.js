class Matrix {
	constructor(size) {
		this.matrix = Array(size).fill(Array(size));
		this.noWayNodes = Array();
		
		let noWayNode = Math.floor(Math.random() * (size - 1 + 1)) + 1
	}
	
	generateNoWayNodes() {
		
	}
}